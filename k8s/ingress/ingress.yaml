# Ingress configuration for external access
# This uses the standard Kubernetes Ingress
# For Azure, you can also use Azure Application Gateway Ingress Controller (AGIC)

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: studojo-ingress
  namespace: studojo
  annotations:
    # For NGINX Ingress Controller
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # cert-manager for automatic TLS certificates
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx  # Change to "azure-application-gateway" if using AGIC
  tls:
  - hosts:
    - studojo.pro
    - www.studojo.pro
    - api.studojo.pro
    - admin.studojo.pro
    - studojo.com
    - www.studojo.com
    - api.studojo.com
    secretName: studojo-tls-secret  # Create this secret with your TLS cert, or use cert-manager
  rules:
  # Frontend
  - host: studojo.pro
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 3000
  - host: www.studojo.pro
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 3000
  - host: studojo.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 3000
  - host: www.studojo.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 3000
  - host: api.studojo.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: control-plane
            port:
              number: 8080
  # Control Plane API
  - host: api.studojo.pro
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: control-plane
            port:
              number: 8080
  # Admin Panel
  - host: studojo.com
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: admin-panel
            port:
              number: 3001
  - host: www.studojo.com
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: admin-panel
            port:
              number: 3001
  - host: studojo.pro
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: admin-panel
            port:
              number: 3001
  - host: www.studojo.pro
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: admin-panel
            port:
              number: 3001
  # Admin Panel - Dedicated Subdomain
  - host: admin.studojo.pro
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: admin-panel
            port:
              number: 3001
