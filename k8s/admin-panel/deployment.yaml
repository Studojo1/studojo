apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-panel
  namespace: studojo
  labels:
    app: admin-panel
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: admin-panel
  template:
    metadata:
      labels:
        app: admin-panel
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: admin-panel
        image: acrstudojo-dhfsdrfhf6a6bbg2.azurecr.io/admin-panel:latest
        ports:
        - containerPort: 3001
          name: http
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: database-url
        - name: VITE_AUTH_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: BETTER_AUTH_URL
        - name: VITE_CONTROL_PLANE_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: CONTROL_PLANE_URL
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: NODE_ENV
        livenessProbe:
          httpGet:
            path: /
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: admin-panel
  namespace: studojo
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: admin-panel

